# Integration tests (placeholder)

Target home for end-to-end API and pipeline integration tests. Today the
integration coverage lives in the flat suite (`test_e2e.py`, `test_api_*.py`,
`test_flux_pipeline.py`) and runs in CI. Migrate/expand here incrementally. See
`../README.md` for the full layout.
