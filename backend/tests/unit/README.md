# Unit tests (placeholder)

Target home for pure-unit tests (parsing, flux math, matching, regression) as they
are split out of the flat `tests/*.py` files. Those files are green and run in CI
today; migrate them here incrementally. See `../README.md` for the full layout.
